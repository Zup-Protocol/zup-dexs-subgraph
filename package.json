{
  "name": "zup-dexs-subgraph",
  "description": "The Zup Protocol Subgraph with protocol supported DEXs",
  "license": "MIT",
  "engines": {
    "node": ">=20.11.1"
  },
  "scripts": {
    "lint": "npx eslint .",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zup-dexs-subgraph",
    "create-local": "graph create --node http://localhost:8020/ zup-dexs-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ zup-dexs-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zup-dexs-subgraph",
    "test": "graph test",
    "prepare": "husky"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "husky": "^9.1.7",
    "matchstick-as": "^0.6.0",
    "typescript-eslint": "^8.18.1"
  }
}